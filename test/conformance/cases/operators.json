{
  "comment": "Comparison and coercion. Seeded from the table captured for the `==` fix, where JavaScript's own `==` diverges from PHP's most sharply.",
  "cases": [
    { "name": "eq/null-vs-false", "template": "{{ (a == false) ? 'T' : 'F' }}", "data": { "a": null } },
    { "name": "eq/undefined-vs-false", "template": "{{ (a == false) ? 'T' : 'F' }}" },
    { "name": "eq/false-vs-null", "template": "{{ (false == null) ? 'T' : 'F' }}" },
    { "name": "eq/null-vs-true", "template": "{{ (null == true) ? 'T' : 'F' }}" },
    { "name": "eq/null-vs-zero", "template": "{{ (null == 0) ? 'T' : 'F' }}" },
    { "name": "eq/null-vs-empty-string", "template": "{{ (null == '') ? 'T' : 'F' }}" },
    { "name": "eq/null-vs-string", "template": "{{ (null == 'a') ? 'T' : 'F' }}" },
    { "name": "eq/null-vs-zero-string", "template": "{{ (null == '0') ? 'T' : 'F' }}" },
    { "name": "eq/null-vs-null", "template": "{{ (null == null) ? 'T' : 'F' }}" },

    { "name": "eq/false-vs-zero", "template": "{{ (false == 0) ? 'T' : 'F' }}" },
    { "name": "eq/false-vs-empty-string", "template": "{{ (false == '') ? 'T' : 'F' }}" },
    { "name": "eq/false-vs-zero-string", "template": "{{ (false == '0') ? 'T' : 'F' }}" },
    { "name": "eq/false-vs-word", "template": "{{ (false == 'false') ? 'T' : 'F' }}" },
    { "name": "eq/false-vs-string", "template": "{{ (false == 'a') ? 'T' : 'F' }}" },
    { "name": "eq/true-vs-word", "template": "{{ (true == 'true') ? 'T' : 'F' }}" },
    { "name": "eq/true-vs-string", "template": "{{ (true == 'a') ? 'T' : 'F' }}" },
    { "name": "eq/true-vs-empty-string", "template": "{{ (true == '') ? 'T' : 'F' }}" },
    { "name": "eq/true-vs-one", "template": "{{ (true == 1) ? 'T' : 'F' }}" },
    { "name": "eq/true-vs-two", "template": "{{ (true == 2) ? 'T' : 'F' }}" },
    { "name": "eq/true-vs-zero", "template": "{{ (true == 0) ? 'T' : 'F' }}" },

    { "name": "eq/int-vs-numeric-string", "template": "{{ (1 == '1') ? 'T' : 'F' }}" },
    { "name": "eq/int-vs-padded-string", "template": "{{ (1 == '01') ? 'T' : 'F' }}" },
    { "name": "eq/numeric-strings", "template": "{{ ('1' == '01') ? 'T' : 'F' }}" },
    { "name": "eq/exponent-strings", "template": "{{ ('10' == '1e1') ? 'T' : 'F' }}" },
    { "name": "eq/zero-vs-word", "template": "{{ (0 == 'foo') ? 'T' : 'F' }}" },
    { "name": "eq/empty-string-vs-zero", "template": "{{ ('' == 0) ? 'T' : 'F' }}" },
    { "name": "eq/word-vs-zero", "template": "{{ ('abc' == 0) ? 'T' : 'F' }}" },
    { "name": "eq/int-vs-float", "template": "{{ (1 == 1.0) ? 'T' : 'F' }}" },
    { "name": "eq/same-strings", "template": "{{ ('abc' == 'abc') ? 'T' : 'F' }}" },
    { "name": "eq/case-differing-strings", "template": "{{ ('abc' == 'ABC') ? 'T' : 'F' }}" },

    { "name": "eq/empty-array-vs-false", "template": "{{ ([] == false) ? 'T' : 'F' }}" },
    { "name": "eq/empty-array-vs-true", "template": "{{ ([] == true) ? 'T' : 'F' }}" },
    { "name": "eq/array-vs-true", "template": "{{ ([1] == true) ? 'T' : 'F' }}" },
    { "name": "eq/same-arrays", "template": "{{ ([1, 2] == [1, 2]) ? 'T' : 'F' }}" },
    { "name": "eq/different-arrays", "template": "{{ ([1, 2] == [3, 4]) ? 'T' : 'F' }}" },
    { "name": "eq/different-length-arrays", "template": "{{ ([1, 2] == [1, 2, 3]) ? 'T' : 'F' }}" },
    { "name": "eq/array-vs-int", "template": "{{ ([1, 2] == 2) ? 'T' : 'F' }}" },
    { "name": "eq/numeric-string-array", "template": "{{ (['1', '2'] == [1, 2]) ? 'T' : 'F' }}" },
    { "name": "eq/empty-hash-vs-false", "template": "{{ ({} == false) ? 'T' : 'F' }}" },
    { "name": "eq/same-hashes", "template": "{{ ({a: 1} == {a: 1}) ? 'T' : 'F' }}" },
    { "name": "eq/different-hash-values", "template": "{{ ({a: 1} == {a: 2}) ? 'T' : 'F' }}" },
    { "name": "eq/different-hash-keys", "template": "{{ ({a: 1} == {b: 1}) ? 'T' : 'F' }}" },
    { "name": "eq/hash-key-order", "template": "{{ ({a: 1, b: 2} == {b: 2, a: 1}) ? 'T' : 'F' }}" },
    { "name": "eq/hash-vs-true", "template": "{{ ({a: 1} == true) ? 'T' : 'F' }}" },

    { "name": "neq/null-vs-false", "template": "{{ (null != false) ? 'T' : 'F' }}" },
    { "name": "neq/null-vs-true", "template": "{{ (null != true) ? 'T' : 'F' }}" },

    { "name": "cmp/spaceship-ints", "template": "{{ 1 <=> 2 }}|{{ 2 <=> 1 }}|{{ 1 <=> 1 }}" },
    { "name": "cmp/less-than-strings", "template": "{{ ('a' < 'b') ? 'T' : 'F' }}" },
    { "name": "cmp/numeric-string-order", "template": "{{ ('10' > '9') ? 'T' : 'F' }}" },

    { "name": "arith/modulo-negative", "template": "{{ -7 % 3 }}" },
    { "name": "arith/int-division", "template": "{{ 7 // 2 }}|{{ -7 // 2 }}" },
    { "name": "arith/power", "template": "{{ 2 ** 10 }}" },
    { "name": "arith/string-concat-null", "template": "{{ 'a' ~ null ~ 'b' }}" }
  ]
}
